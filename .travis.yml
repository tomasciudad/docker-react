sudo: required
languague: generic

services:
 - docker

before_install:
 - docker build -t tomasciudad/docker-react -f Dockerfile.dev .

script:
 - docker run -e CI=true tomasciudad/docker-react npm run test
